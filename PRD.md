# 簡易截圖小工具：FastShot

以 Python 實作，快捷鍵為主的跨平台截圖工具，工具命令為 fastshot。

## 使用介面

- 執行後先隱藏編輯視窗，僅在系統匣顯示圖示，圖示以照相機形象呈現
- 在圖上按滑鼠右鍵顯示功能表，僅有結束可用
- 雙按圖示可以顯示編輯視窗

## 可用的截圖快捷鍵

macOS 把 alt 換成 opt：

- alt+shift+a 擷取目前焦點的視窗
- alt+shift+r 擷取選取矩形區域
- alt+shift+f 擷取全螢幕
- alt+shift+w 擷取選取的視窗或視窗內的控制元件，應以游標所在位置下的最小視窗為目標

## 截圖流程

1. 按下快捷鍵先隱藏編輯視窗
2. 依照快捷鍵種類截圖，可按 ESC 取消
3. 截圖後直接複製到系統剪貼簿
4. 以編輯視窗內的新頁籤顯示截圖內容，頁籤標題格式為 yy-mm-dd-hhmmss
5. 尚未存檔或是編輯過的截圖都要在頁籤顯示待存檔的標記

## 編輯視窗

- 現代扁平風格的多頁籤式介面
- 視窗標題列顯示 "FastShot-<目前頁籤的標題>"，若目前沒有任何頁籤，就顯示 "FastShot"
- 頁籤數量較多無法全部呈現在標籤列時，最右側要有下拉清單可以選頁籤。如果按位於標籤列兩端的標籤時，應該要把標籤列往視窗中間捲，讓兩端外的標籤捲進來變成可以操作
- 編輯視窗縮成最小時要變成隱藏，只會在系統匣顯示圖示
- 標籤列上方要有工具列，以圖示的方式放入以下工具：
    - 鉛筆（快捷鍵 alt+p）：繪製自由筆畫
    - 線條（快捷鍵 alt+l）：繪製直線
    - 矩形（快捷鍵 alt+r）：繪製矩形
    - 文字（快捷鍵 alt+t）：繪製文字，滑鼠點按置放位置後出現文字輸入面板，除可輸入文字外，還可選用字體，輸入文字按 Enter 才放置
    - 馬賽克（快捷現 alt+m）：幫選取區域加上馬賽克效果，避免洩漏機密資訊
    - 線條粗細與顏色（快捷鍵 alt+c）：按下後顯示設定面板，可以設定線條粗細與顏色，這會影響前面繪圖工具的線條以及文字的字體，工具圖示要即時顯示目前設定的線條粗細與顏色
    - 復原（依平台慣用的復原快捷鍵）：上述編輯工具都要能被復原
    - 複製（依平台慣用的複製快捷鍵）：將目前編輯區內容複製到剪貼簿
    - 存檔（依平台慣用的存檔快捷鍵）：
        - 若當前頁籤尚未存檔，就等同另存新檔，預設檔名就是標籤頁標題
        - 若當前頁籤已存過檔，但有編輯過，就直接以目前檔名存檔
        - 若當前頁籤已存過檔，沒有再編輯，此工具無法使用
    - 另存新檔（依平台慣用另存新檔快捷鍵）：
        - 顯示儲存檔案交談窗供使用者選取存檔位置與檔名，預設檔名就是目前頁籤標題，若為已存檔的標籤，以原本存檔位置與檔名為預設值
        - 可選擇儲存成 PNG 或是 JPG
        - 存檔後頁籤標題顯示檔名
- 放大比例（依平台慣用放大比例快捷鍵）：放大顯示比例，預設 100%
- 縮小比例（依平台慣用縮小比例快捷鍵）：縮小顯示比例，預設 100%
- 包含滑鼠游標（不需快捷鍵）：截圖時是否要包含游標
- 延遲（不需快捷鍵）：提供不延遲，1,3,5 秒以及自訂秒數

## 注意事項

- 快捷鍵應該要被吃掉，不要送給具有輸入焦點的視窗，造成多餘的輸入而出錯，像是把自己開啟的預覽視窗關掉